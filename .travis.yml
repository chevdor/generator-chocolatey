language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-chocolatey' ]; then cd .. && eval "mv $currentfolder generator-chocolatey" && cd generator-chocolatey; fi

